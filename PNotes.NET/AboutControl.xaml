<UserControl x:Class="PNotes.NET.AboutControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Loaded="UserControl_Loaded">
    <Grid>
        <Canvas Name="canvas" ClipToBounds="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel Name="panel" Width="{Binding ActualWidth, ElementName=canvas}" MouseEnter="panel_MouseEnter"  MouseLeave="panel_MouseLeave">

            </StackPanel>
        </Canvas>
    </Grid>
</UserControl>
